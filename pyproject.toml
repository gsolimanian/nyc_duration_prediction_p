[project]
name = "nyc-prediction-prediction-p"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
nyc-prediction-prediction-p = "nyc_prediction_prediction_p:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
